import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: 'light',
    white: {
      main: '#FFFFFF'
    },
    color1: {
      main: '#08777a',
    },
  }
})

export default theme
