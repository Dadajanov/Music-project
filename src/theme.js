import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: 'dark',
    white: {
      main: '#FFFFFF'
    },
    color1: {
      main: '#08777a',
    },
    color2: {
      main: '#971010',
    },
  }
})

export default theme
